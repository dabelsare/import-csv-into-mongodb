{
  "name": "import-csv-into-mongodb",
  "version": "1.0.0",
  "description": "This is import csv file into mongodb database example",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["import CSV into MongoDB", "CSV into MongoDB", "CSV convert into json and save into MongoDB", "CSV bulk import into MongoDB database"],
  "author": "Dipak Belsare",
  "license": "ISC",
  "dependencies": {
    "csvtojson": "^2.0.10",
    "mongodb": "^3.6.6"
  }
}
